public record Student(String name, String grade, String major, double gpa ) {
    // record is a data-only class
    // enter the field in the parameter, and the compiler will automatically create the getters
}
